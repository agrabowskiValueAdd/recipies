import { PrepTimePipePipe } from './prep-time-pipe.pipe';

describe('PrepTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PrepTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
